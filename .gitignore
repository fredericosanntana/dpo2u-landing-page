node_modules/
.next/
.turbo/
*.log
dist/
build/
node_modules/

# Secrets — never commit. Use .env.example as template.
.env
.env.*
!.env.example

# artefato incremental do tsc — muda a cada build, não é fonte (add 2026-08-04)
*.tsbuildinfo
