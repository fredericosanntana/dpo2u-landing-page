// STUB — original perdido, aguardando reescrita ou backup externo.
// Recovery 2026-05-14: return null. Hero importa esse componente com
// props { open, onClose } — signature mantida pra build não quebrar.

interface PitchDeckModalProps {
  open: boolean;
  onClose: () => void;
  deckUrl?: string;
}

export default function PitchDeckModal(_props: PitchDeckModalProps) {
  return null;
}
